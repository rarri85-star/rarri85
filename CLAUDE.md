# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This is an [Obsidian](https://obsidian.md/) knowledge management vault located at `C:\Users\Owner\Documents\옵시디언\Obsidian`. It is not a software project — there are no build steps, tests, or compilation. Work here consists of creating and editing Markdown notes.

## Vault Configuration

- **Theme:** Minimal (dark mode via `obsidian` base theme)
- **Sync:** Obsidian Sync is enabled
- **Community plugins:** `terminal` (Obsidian Terminal — embedded terminal pane within Obsidian)

## Core Plugins Enabled

Notable enabled core plugins: file-explorer, global-search, graph, backlink, canvas, daily-notes, templates, note-composer, command-palette, properties, outline, bookmarks, bases, file-recovery.

Disabled: slash-command, markdown-importer, zk-prefixer, slides, audio-recorder, workspaces, webviewer.

## Note Format

Notes are plain Markdown (`.md`). Obsidian-specific syntax in use:
- `[[wikilinks]]` for internal links between notes
- YAML frontmatter for note properties (supported via the `properties` core plugin)
- Canvas files (`.canvas`) for visual boards

## Working in This Vault

When creating or editing notes, place them directly in the vault root or in subdirectories — Obsidian picks them up automatically. The `.obsidian/` directory contains vault configuration; avoid editing it manually unless adjusting specific plugin settings.
